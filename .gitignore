# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.env
*.claude
*.mcp.json
.playwright-mcp

# Local project memory and its agent entry point
/.local-context/
/AGENTS.md

# Local Codex MCP config generated from .vscode/mcp.json
.codex
.vscode

# Appwrite config (contains project IDs / not for VCS)
appwrite.config.json

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# 2FA E2E — local OTP handshake scratch (never commit codes)
tests/e2e/2fa/.otp/

# Local self-hosted Appwrite stack (installer output; .env holds secret keys)
/appwrite/

# C2PA signing identity — the private key never leaves the machine that made it
functions/sign-photo/.certs/
functions/sign-photo-rs/.certs/

# Rust build output
functions/sign-photo-rs/target/
